# Walkthrough: Release & Deploy Phiên Bản v3.41 (Khôi Phục Trạng Thái Gốc Tinh Gọn Trước Khi Triển Khai 3 Tầng)

**Thời gian thực hiện:** 17/08/2026 00:00:05 (Giờ địa phương)  
**Tài liệu đối chiếu:** [chat.md](file:///D:/customLooker/chat.md)  
**Phạm vi file:**
- [src/index.js](file:///D:/customLooker/src/index.js)
- [src/export.js](file:///D:/customLooker/src/export.js)
- [downloader.html](file:///D:/customLooker/downloader.html)
- [package.json](file:///D:/customLooker/package.json)
- [manifest.json](file:///D:/customLooker/manifest.json)
- [webpack.config.js](file:///D:/customLooker/webpack.config.js) / [index.bundle.v3.41.js](file:///D:/customLooker/index.bundle.v3.41.js)
- [index.v3.41.css](file:///D:/customLooker/index.v3.41.css) / [index.v3.41.json](file:///D:/customLooker/index.v3.41.json)

---

## 🎯 Mục Tiêu Của Bản Release v3.41

Theo yêu cầu của User:
- **Khôi phục lại chính xác trạng thái gốc của `src/index.js` qua Git (`git restore`)**: Hoàn toàn loại bỏ tất cả các cơ chế watchdog, retry pings, reload iframe hoặc resubscribe.
- Quay về đúng cơ chế subscriber gốc tinh gọn chuẩn của Looker Studio:
  ```javascript
  // HÀM NHẬN DỮ LIỆU TỪ LOOKER STUDIO (BATCHING VỚI RAF ĐỂ CHỐNG LAG RESIZE)
  let renderRafId = null;
  function drawVisualization(data) {
      try {
          if (currentData !== data) {
              processedRowsCache.dataRef = null;
              processedRowsCache.key = '';
              processedRowsCache.rows = null;

              summaryCache.rowsRef = null;
              summaryCache.key = '';
              summaryCache.result = null;
          }

          currentData = data;
          if (renderRafId) cancelAnimationFrame(renderRafId);
          renderRafId = requestAnimationFrame(() => {
              renderTable();
          });
      } catch (err) {
          console.error('[ExcelViz] drawVisualization error:', err);
      }
  }

  // ĐĂNG KÝ SUBSCRIBER ĐỂ NHẬN DỮ LIỆU TỪ LOOKER STUDIO
  try {
      dscc.subscribeToData(drawVisualization, { transform: dscc.tableTransform });
  } catch (e) {
      console.error('[ExcelViz] dscc subscribe error:', e);
  }
  ```
- Đồng thời **giữ trọn vẹn toàn bộ các tối ưu hiệu năng & RAM cốt lõi**:
  - Cache Filter/Sort + Cache Summary Row.
  - Xóa cache khi nhận dataset mới.
  - Debounce ô tìm kiếm 250ms.
  - Phân trang an toàn khi `totalRows > 5,000`.
  - SheetJS v0.20.3 Dense Worksheet + Chunking CSV + Chống Formula Injection.
  - Giải phóng RAM sau khi export $> 200\text{k}$ dòng.

---

## 🚀 Kết Quả Build & Deploy Production

1. **Build Webpack:**
   ```text
   asset index.bundle.v3.41.js 56.3 KiB [emitted] [minimized] (name: main)
   webpack 5.109.2 compiled successfully in 789 ms
   ```
2. **Deploy GCS Bucket (`gs://analytics_merap/excelchart3/`):**
   - `index.bundle.v3.41.js`
   - `index.v3.41.css`
   - `index.v3.41.json`
   - `downloader.html`
   - `index.json`
   - `manifest.json` (Trỏ `v3.41`, `"devMode": false`)
   - Kết quả: `Operation completed over 6 objects/187.3 KiB.` (100% Done).
3. **Gói Nén Dự Án:** `customLooker.zip` (408 KB).
