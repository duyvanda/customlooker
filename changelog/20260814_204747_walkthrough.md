# Walkthrough: Custom Looker Studio Table Viz - Uncheck All Search Columns by Default

## 1. Vấn đề gốc rễ & Các giải pháp đã triển khai

1. **Thiết lập mặc định Uncheck All cho cột `🔍 Tìm` trong Modal (`image copy 5.png`):**
   - Đã điều chỉnh giá trị mặc định của `searchable: false` cho toàn bộ các cột.
   - Khi mở Modal cấu hình lần đầu hoặc thêm cột mới, tất cả checkbox trong cột **`🔍 Tìm`** sẽ ở trạng thái **bỏ chọn (Unchecked)**.
   - **Cơ chế tìm kiếm thông minh:**
     - Khi **chưa tích chọn cột nào** (Uncheck all): Ô Search sẽ tìm kiếm trên **toàn bộ tất cả các cột đang hiển thị** (placeholder hiển thị: `Tìm kiếm nhanh...`).
     - Khi bạn **tích chọn 1 hoặc nhiều cột cụ thể**: Ô Search sẽ chỉ quét dữ liệu trên đúng các cột được tích chọn (placeholder tự động cập nhật: `Tìm theo: <Tên cột>...`).
2. **Đóng gói dự án & Git:**
   - Cập nhật [`customLooker.zip`](file:///D:/customLooker/customLooker.zip).
   - Push lên GitHub [https://github.com/duyvanda/customlooker.git](https://github.com/duyvanda/customlooker.git).
   - Deploy GCS `gs://analytics_merap/excelchart2/`.

---

## 2. Danh sách file trong gói ZIP & Dự án

- [`src/index.js`](file:///D:/customLooker/src/index.js): Mặc định uncheck all cho checkbox `🔍 Tìm`, Hệ thống lưu trữ đa tầng Multi-layer persistence, Cột `Sắp xếp (Sort)`, Phân trang 10 cột, 3-state sorting, Formatter.
- [`manifest.json`](file:///D:/customLooker/manifest.json): Khai báo `"devMode": true` để Looker Studio luôn nạp code mới từ GCS.
- [`downloader.html`](file:///D:/customLooker/downloader.html): Giao diện tải file Excel, CSV & Copy JSON.
- [`index.css`](file:///D:/customLooker/index.css): Căn trên cho modal overlay (`flex-start`), Modal pagination styles, màu đen đậm `#000000`.
- [`index.json`](file:///D:/customLooker/index.json): Khai báo Data Setup chuẩn quốc tế (`Dimension`, `Metric`) và Style Panel.
- [`package.json`](file:///D:/customLooker/package.json): Cấu hình dependencies npm.
- [`webpack.config.js`](file:///D:/customLooker/webpack.config.js): Cấu hình đóng gói Webpack.
- [`index.bundle.js`](file:///D:/customLooker/index.bundle.js): File bundle đã build sẵn để deploy (~46.3 KB).
- [`agent.md`](file:///D:/customLooker/agent.md): Tài liệu toàn diện về toàn bộ quy tắc hoạt động, cấu hình, formatter và kiến trúc của biểu đồ.

---

## 3. Trạng thái triển khai

- **Google Cloud Storage:** Đã upload toàn bộ file với quyền `public-read` lên `gs://analytics_merap/excelchart2/`.
- **GitHub Repository:** Đã đẩy toàn bộ mã nguồn lên [https://github.com/duyvanda/customlooker.git](https://github.com/duyvanda/customlooker.git) (branch `main`).
