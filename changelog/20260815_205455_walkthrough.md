# 📋 Walkthrough Nhật Ký Thay Đổi — 2026-08-15 21:52:15 (Tối ưu hóa Popup & Không cần F5)

## 🎯 Mục Tiêu & Cải Tiến Đột Phá
1. **Tối ưu hóa Popup Blocker (Không cần F5 trang):**
   - **Đồng bộ Token User Gesture:** Mở cửa sổ helper ngay dòng đầu tiên của sự kiện click (`window.open('', '_blank')`) trước khi chạy vòng lặp xử lý dữ liệu nặng. Giúp trình duyệt nhận diện 100% đây là thao tác click trực tiếp từ người dùng, giảm thiểu tối đa khả năng bị chặn popup.
   - **Modal Trợ Giúp Tải Ngay (In-App Fallback):** Nếu trình duyệt có chính sách chặn popup quá nghiêm ngặt, hệ thống sẽ hiện modal thông báo thân thiện kèm nút **"🚀 Mở Tab Tải File Ngay"** để người dùng bấm tải trực tiếp **mà hoàn toàn không cần phải F5 / tải lại trang**.
2. **Tái cấu trúc (Refactor) `src/index.js` thành các module phẳng trực tiếp trong thư mục `src/`:**
   - `src/formatters.js`, `src/schema.js`, `src/rules.js`, `src/sorters.js`, `src/summary.js`, `src/ui.js`, `src/export.js`, `src/index.css`.
3. **Dọn dẹp toàn bộ file `.LICENSE.txt` và bundle cũ** chuyển vào `archive/`.
4. **Cập nhật tài liệu quy trình vận hành trong [`AGENTS.md`](file:///D:/customLooker/AGENTS.md).**
5. **Hoàn tất deploy phiên bản `v3.25` lên GCS Production và GitHub repo.**

---

## 🛠️ Chi Tiết Cấu Trúc Module Phẳng Trong `src/`

```text
src/
├── index.js              # Entry point chính: điều phối dscc lifecycle, pagination, renderTable
├── formatters.js         # Engine BigQuery Date/Time (%Y,%m,%d...), Number (%'.2f, vnd...), escapeHtml, remove_accents
├── schema.js             # Bóc tách metadata DSCC Looker Studio (tableColumns, searchColumns, sortConfig)
├── rules.js              # Conditional formatting (Rule 1-3) & Column Color Groups 1-3
├── sorters.js            # Natural multi-type comparator (compareValues)
├── summary.js            # Tính toán Dòng Tổng Hợp trên sortedRows (3 cấp ưu tiên) & DOM generator
├── ui.js                 # Sticky freeze offsets, Drag-to-resize độ rộng cột, Popup ẩn/hiện cột
├── export.js             # Giao tiếp helper tab downloader.html (postMessage) & extract Date Range
└── index.css             # Source CSS chính
```

---

## 🚀 Trạng Thái Deploy & Triển Khai v3.25

- **GCS Target:** `gs://analytics_merap/excelchart3/`
- **File Versioning:**
  - `index.bundle.v3.25.js` (Webpack Bundle 51.7 KiB)
  - `index.v3.25.css`
  - `index.v3.25.json`
  - `downloader.html`
  - `index.json`
  - `manifest.json` (Trỏ `v3.25`, `devMode: false`)
- **Google Edge CDN:** Đã kích hoạt (`Cache-Control: no-cache, no-store, must-revalidate`).
- **GitHub Repository:** Đã commit và push thành công lên `main`.
