# 📋 Walkthrough Nhật Ký Thay Đổi — 2026-08-15 22:29:45 (v3.26)

## 🎯 Mục Tiêu & Cải Tiến Đột Phá
1. **Hoàn thiện bộ sắp xếp Ngày Tháng (Chronological Timestamp Sort):**
   - Tạo hàm `getDateSortKey(val, fieldType)` trong `src/formatters.js` chuẩn hóa mọi định dạng ngày tháng sang chuỗi 14 chữ số thời gian thực tế `YYYYMMDDHHMMSS`.
   - Khắc phục hoàn toàn lỗi sort text đẩy số `0` lên đầu bảng hoặc so sánh theo ký tự ngày (`01` đến `09` bị nhảy lên trước).
   - Hỗ trợ so sánh chi tiết chính xác đến từng giây đối với DateTime có `HH:mm:ss`.
   - Tự động đẩy giá trị `0` / rỗng / không hợp lệ xuống cuối danh sách.
2. **Tối ưu hóa Popup Blocker (Không cần F5 trang):**
   - Đồng bộ Token User Gesture ngay khi nhấp chuột.
   - Hiển thị Modal thông báo kèm nút **"🚀 Mở Tab Tải File Ngay"** để tải trực tiếp nếu trình duyệt chặn popup, không cần tải lại trang.
3. **Tái cấu trúc (Refactor) `src/index.js` thành các module phẳng trực tiếp trong thư mục `src/`:**
   - `src/formatters.js`, `src/schema.js`, `src/rules.js`, `src/sorters.js`, `src/summary.js`, `src/ui.js`, `src/export.js`, `src/index.css`.
4. **Dọn dẹp toàn bộ file `.LICENSE.txt` và bundle cũ** chuyển vào `archive/`.
5. **Cập nhật tài liệu quy trình vận hành trong [`AGENTS.md`](file:///D:/customLooker/AGENTS.md).**
6. **Hoàn tất deploy phiên bản `v3.26` lên GCS Production và GitHub repo.**

---

## 🛠️ Chi Tiết Cấu Trúc Module Phẳng Trong `src/`

```text
src/
├── index.js              # Entry point chính: điều phối dscc lifecycle, pagination, renderTable
├── formatters.js         # Engine BigQuery Date/Time (%Y,%m,%d...), Number (%'.2f, vnd...), getDateSortKey, escapeHtml, remove_accents
├── schema.js             # Bóc tách metadata DSCC Looker Studio (tableColumns, searchColumns, sortConfig)
├── rules.js              # Conditional formatting (Rule 1-3) & Column Color Groups 1-3
├── sorters.js            # Natural multi-type comparator (compareValues) với Chronological Date Sort
├── summary.js            # Tính toán Dòng Tổng Hợp trên sortedRows (3 cấp ưu tiên) & DOM generator
├── ui.js                 # Sticky freeze offsets, Drag-to-resize độ rộng cột, Popup ẩn/hiện cột
├── export.js             # Giao tiếp helper tab downloader.html (postMessage) & extract Date Range
└── index.css             # Source CSS chính
```

---

## 🚀 Trạng Thái Deploy & Triển Khai v3.26

- **GCS Target:** `gs://analytics_merap/excelchart3/`
- **File Versioning:**
  - `index.bundle.v3.26.js` (Webpack Bundle 52.5 KiB)
  - `index.v3.26.css`
  - `index.v3.26.json`
  - `downloader.html`
  - `index.json`
  - `manifest.json` (Trỏ `v3.26`, `devMode: false`)
- **Google Edge CDN:** Đã kích hoạt (`Cache-Control: no-cache, no-store, must-revalidate`).
- **GitHub Repository:** Đã commit và push thành công lên `main`.
